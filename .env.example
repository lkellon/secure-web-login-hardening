# Example environment variables for the secure login demonstration.
# Do NOT place real credentials in this file.

DB_HOST=localhost
DB_NAME=example_db
DB_USER=app_user
DB_PASSWORD=replace_with_secure_password
